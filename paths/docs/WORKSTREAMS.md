# Paths workstreams

**Completed capability: a second mathematical family through the same recipe pipeline.**
`tools/author_question_family.py` provides `init` and `check` for
`linear_balance_v1` and `sine_turn_v1`. Writers edit the same four data/prose
files. The runner owns IDs, numbering, set labels, choice placement, links,
metadata, compiler/model checks and the compact verification handoff.

The sine provider is a 110-line adapter for finite seeds and calculated
teaching fields. It reuses the frozen Wave 01 exact mathematical functions;
both Markdown templates initially match the reviewed sine source byte for
byte. No old sine recipe reader, presentation or packaging function is called.
The imported mathematical implementation is included in verification hashes
and source-change rejection. Published reproduction sources remain frozen.
The linear provider's former standalone packaging route remains removed.

Correct positions now vary across sets as well as roles, keeping each choice
attached to its own feedback. Each family produces one 18-question chapter
with one shared lesson, 21 decisions and 42 wrong-choice corrections through
the same compiler, model and exporter owners. No runtime grammar, judge,
renderer or 3D source changed. This capability adds one production Python
file and 121 net production Python lines; the existing family tests were
consolidated into one common test file, without a second test runner.

All **18 tests** in the single targeted `paths_question_family_tests` CTest
entry pass. The mathematics checks cover 512 linear per-set seed combinations
and 275 permitted sine per-role seeds. They also check unchanged reviewed sine
mathematics/teaching, actual Markdown and number edits, source-bound dependency
hashes, balanced positions, false keys/working and invalid inputs. Sine errors
identify the exact role/case field. Changing an undisplayed reflection offset
cannot masquerade as new practice.

Both families import together into a copy of the full library, reaching
**737 questions** while preserving all 701 earlier stamps and document/package
bytes. Saved-progress replay and unchanged repeat imports pass. The live
library is unchanged. The examples remain unpublished; the default sine cases
deliberately reproduce reviewed practice and do not add mathematical coverage.

- [Recipe authoring and exact commands](CONTENT_PRODUCTION.md#recipe-authoring)
- [Editable sine recipe](../content/authoring/learning/sine_family/recipe.json)
- [Sine question template](../content/authoring/learning/sine_family/questions.paths.md.in)
- [Sine lesson template](../content/authoring/learning/sine_family/lesson.md.in)
- [Sine teaching contract and field reference](../content/authoring/learning/sine_family/DESIGN.md)
- [Linear teaching contract](../content/authoring/learning/linear_family/DESIGN.md)
- [Capability evidence](../build/sine-recipe-evidence/verification.json)

The live baseline is completed Wave 04: **701 questions, 979 readings,
1,290 sections and 44 structured lessons in 51 documents**. Its 48 new
questions and four lessons, all preceding published source inventories, and
saved-progress preservation are bound in
[completion.json](../build/production/wave04/completion.json). All four writer
assignments are closed. Source-assisted production did not establish a token,
speed or weekly-allowance saving; observed effort remains in its measurement
record. No subsequent curriculum wave is assigned.

Work stays uncommitted and preserves separate 3D work. No screenshots, captures,
windows, font/ImGui probes or clipboard access. Routine content QA belongs to
the coordinator; visual quality and learner outcomes remain unobserved.

The next candidate is one reviewed calculus family using the same recipe
and checking entry point. It is not assigned to a worker or a production wave.
Full curriculum coverage, generic four-level support and demonstration
packaging remain separate unfinished capabilities.
