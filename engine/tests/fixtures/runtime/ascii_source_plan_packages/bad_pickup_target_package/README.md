# Bad Pickup Target Package

Package wrapper for a negative source-plan scenario. The package manifest is
valid, but `scenario.toml` intentionally references a missing pickup target so
the delegated source-plan validation path fails.
